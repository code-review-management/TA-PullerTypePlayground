import { Dict, Indexable, unknown } from './core';
export declare function dict<T = unknown>(): Dict<T>;
export declare function has(d: Dict, key: string): boolean;
export declare function isIndexable(v: unknown): v is Indexable;
